export type TimeRange = { startTime: number; endTime: number };
export type SkillTimings = {
    move?: TimeRange;
    attack?: TimeRange;
    skill1?: TimeRange;
    skill2?: TimeRange;
    skill3?: TimeRange;
};



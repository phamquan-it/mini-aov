// features/aov/aovTypes.ts
export interface AOVSettings {
    scale: number;
    // You can add more AOV settings here
    fov?: number;
    zoom?: number;
    minScale?: number;
    maxScale?: number;
}
